import { ParseArrayNumberPipe } from './parse-array-number.pipe';

describe('ParseArrayNumberPipe', () => {
  it('should be defined', () => {
    expect(new ParseArrayNumberPipe()).toBeDefined();
  });
});
